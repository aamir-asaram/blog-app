<!DOCTYPE html>
<html class="bg-violet-100 flex justify-center content-center">
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="w-[90%] flex flex-col content-center">
    <main class="pt-8">
      <% if user_signed_in? %>
        <%= button_to "Log out", destroy_user_session_path, method: :delete, class: "ml-[92%] py-2 px-4 bg-blue-200 rounded-full" %>
      <% else %>
        <% unless controller_name == 'registrations' || controller_name == 'sessions' %>
          <%= link_to "Log in", new_user_session_path, class: "ml-[88%] py-2 px-4 bg-blue-200 rounded-full" %>
          <%= link_to "Sign up", new_user_registration_path, class: "mt-4 py-2 px-4 bg-green-500 rounded-full" %>
        <% end %>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
